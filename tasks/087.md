---
id: 87
parent_id: 80
title: 'Task 1: MarkdownStorage ID Allocation'
status: done
priority: high
type: bug
created_at: "2026-07-10T21:12:57Z"
updated_at: "2026-07-10T21:19:44Z"
---

Files
- Modify: `internal/storage/markdown.go`
- Test: `internal/storage/storage_test.go`

Steps
1. Extend `MarkdownStorage.NextID()` so it returns one greater than the maximum task ID found in both active task files under `s.dir` and archived task files under `filepath.Join(s.dir, "archive")`.
2. Keep the archive no-index design: do not add archived tasks to `.index.json`, do not cache archive state, and do not change `LoadAllArchived()` ordering/behavior.
3. Add a small private helper if it keeps the scan readable, for example a helper that scans one directory for `*.md` filenames, parses numeric basenames with `strconv.Atoi`, ignores malformed names, and returns the maximum ID found. The helper must treat a missing directory as max `0` and return real read errors.
4. Update `TestMarkdownStorage_NextID` with regression coverage for:
   - empty active directory with archived tasks, expecting archived max + 1;
   - mixed active and archived tasks where the highest ID is archived;
   - mixed active and archived tasks where the highest ID is active, to confirm active behavior is preserved.
5. Do not change save/load/archive lookup behavior beyond ID allocation.

Code guidance
- Preserve existing `NextID() (int, error)` signature.
- Existing active-only logic can become two directory scans:
  - scan `s.dir` for active task markdown files;
  - scan `filepath.Join(s.dir, "archive")` for archived task markdown files;
  - return `max(activeMax, archiveMax) + 1`.
- Continue to ignore directories, non-`.md` files, `.index.json`, and non-numeric markdown basenames.
- In tests, use `makeTestTask(id)`, `s.Save(task)`, and `s.Archive(id)` so archived fixtures use real markdown files.

Verification command
```bash
rtk go test ./internal/storage -run 'TestMarkdownStorage_NextID|TestMarkdownStorage_LoadAllArchived|TestMarkdownStorage_LoadArchived|TestMarkdownStorage_IsArchived' | tail -c 4000
```

Commit message
```text
fix(storage): include archived markdown tasks in next id scan
```