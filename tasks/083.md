---
id: 83
parent_id: 79
title: 'Task 3: Packaged Workflow And Command Sync'
status: todo
priority: high
type: feature
created_at: "2026-07-10T20:21:50Z"
updated_at: "2026-07-10T20:21:50Z"
---

Synchronize the packaged workflow skill and both command descriptions with the implicit-role workflow behavior.

Files:
- Modify: `plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md`
- Modify: `commands/execute-all.md`
- Modify: `plugins/mcp-task-manager/commands/execute-all.md`
- Source of truth: `skills/superpowers-workflow/SKILL.md`
- Reference: `docs/plans/2026-07-10-implicit-subagent-roles-migration.md`

Steps:
1. Copy the finalized development workflow skill from `skills/superpowers-workflow/SKILL.md` to `plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md` unless an implementation-time packaging rule requires a documented difference.
2. Verify the two workflow skill copies are behaviorally identical and preferably byte-identical.
3. Update `commands/execute-all.md` so expected setup no longer tells users to run `$install-agents` or register `planner`, `coder`, and `reviewer` custom agents.
4. Update `commands/execute-all.md` workflow summary so it says parent tasks dispatch ordinary subagents with complete planner prompts, implementation subtasks dispatch ordinary subagents with complete coder prompts, and review uses ordinary subagents with complete reviewer prompts when required.
5. State in `commands/execute-all.md` that model/reasoning choices are capability-based recommendations used only when the active `spawn_agent` schema supports them and not overridden by user/runtime constraints.
6. Apply the same command changes to `plugins/mcp-task-manager/commands/execute-all.md`.
7. Ensure both command copies are byte-identical unless an implementation-time packaging rule requires a documented difference.

Code guidance:
- Command docs should be concise user-facing dispatch documentation, not a second full copy of the workflow skill.
- Remove fallback/remediation language tied to custom role-agent discovery.
- Keep the command focused on executing pending tasks through the packaged `superpowers-workflow` skill.

Verification command:
```bash
cmp skills/superpowers-workflow/SKILL.md plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md
cmp commands/execute-all.md plugins/mcp-task-manager/commands/execute-all.md
rtk rg -n 'install-agents|custom role agent|custom agent|\.codex/agents|~/.codex/agents|planner.*registered|coder.*registered|reviewer.*registered' commands plugins/mcp-task-manager/commands plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md | head -c 6000
```
Expected: both `cmp` commands pass; obsolete custom-agent/install references are absent from the command copies and packaged workflow skill.

Commit message:
```text
docs(plugin): sync implicit workflow package docs
```