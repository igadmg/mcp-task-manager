---
id: 68
parent_id: 67
title: 'Task 1: Add parent-priority ordering regression tests'
status: done
priority: high
type: bug
created_at: "2026-04-13T19:07:05Z"
updated_at: "2026-04-13T19:37:23Z"
---

Files:
- Modify: `internal/storage/storage_test.go`
- Reference only: `internal/storage/index.go`

Steps:
1. Add regression coverage around `Index.NextTodo()` in `internal/storage/storage_test.go` using the existing temp-dir + `NewMarkdownStorage`/`NewIndex` pattern.
2. Build fixtures with explicit `CreatedAt` timestamps so parent ordering is deterministic. Cover at least these cases:
   - A todo parent and a standalone top-level task have equal priority, but the parent is older; `NextTodo()` must return the child of the older parent instead of the standalone task.
   - A child has lower own priority than a competing standalone task, but its parent outranks the standalone; `NextTodo()` must still return that child.
   - A winning parent has multiple actionable children; once that parent group wins, the selected child must be chosen by child-level ordering within the group (priority, then created time, then ID).
3. Keep the new tests narrowly focused on the ordering bug. Reuse or extend the existing `TestIndex_NextTodo_*` naming style so the failure clearly points to parent-aware ordering.
4. Run the targeted storage tests and confirm the new regression test fails against the current implementation before handing off to Task #69.

Code guidance:
- Put the new tests near the existing `NextTodo` tests in `internal/storage/storage_test.go`.
- Use fixed UTC timestamps instead of `time.Now()` where parent age ordering matters.
- Assert returned task IDs, not just titles, so tie-breaking behavior is explicit.
- Do not change production code in this task; this task ends with failing regression coverage that captures the bug.

Verification command:
- `go test ./internal/storage -run 'TestIndex_NextTodo_(UsesParentOrderingForTodoParentSubtasks|UsesParentOrderingBeforeChildPriority|SortsWinningParentChildrenAfterParentSelection)$'`

Commit message:
- `test: add regression coverage for parent-aware next task ordering`