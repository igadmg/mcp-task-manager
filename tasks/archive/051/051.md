---
id: 51
parent_id: 49
title: 'Task 2: Keep empty projects fileless during index initialization'
status: done
priority: high
type: bug
created_at: "2026-03-26T21:21:33Z"
updated_at: "2026-03-26T21:50:34Z"
---

Files:
- Modify: `internal/storage/index.go`

Steps:
1. Write or use the failing regression tests from Task 1 as the red state.
2. Run test to verify failure: `go test ./internal/storage ./internal/task -run 'TestIndex_Load|TestService_Initialize' -v`
3. Implement minimal code in `internal/storage/index.go` to avoid persisting `.index.json` when there are no active task markdown files.
4. Run test to verify pass: `go test ./internal/storage ./internal/task -run 'TestIndex_Load|TestService_Initialize' -v`
5. Commit with message: `fix: avoid creating empty index on startup`

Code:
- Add a helper that initializes an empty in-memory index by resetting `idx.entries`, `idx.relationsBySource`, and `idx.relationsByTarget` without calling `Save()`.
- Update `Load()` so a missing `.index.json` only triggers a rebuild-and-save when active task markdown files exist.
- Ensure corruption/staleness fallback does not write a new `.index.json` when the active task set is empty.
- Preserve existing create/update/delete behavior so the first successful task creation still writes the markdown file and then saves the index.

Verification:
`go test ./internal/storage ./internal/task -run 'TestIndex_Load|TestService_Initialize' -v`