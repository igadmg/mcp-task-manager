---
id: 65
parent_id: 63
title: 'Task 1: Add deterministic task ID tie-breaker to next-task selection'
status: done
priority: high
type: bug
created_at: "2026-04-08T10:50:51Z"
updated_at: "2026-04-08T10:53:00Z"
---

Files:
- Modify: `internal/storage/index.go`
- Test: `internal/storage/storage_test.go`
- Verify related expectations still hold in `internal/task/service_test.go` if needed, but do not expand scope unless the existing service tests fail because of the ordering change.

Steps:
1. Add a focused regression test in `internal/storage/storage_test.go` covering two viable todo tasks with the same priority and identical `CreatedAt` timestamps; assert that `NextTodo()` returns the lower numeric task ID.
2. Run the targeted storage test first to confirm the new regression fails against the current implementation.
3. Update the `NextTodo()` candidate sort in `internal/storage/index.go` so ordering is: priority, then `CreatedAt`, then numeric `ID` ascending.
4. Re-run the targeted storage test and then the broader task-selection test set to confirm no existing `NextTodo`/`GetNextTask` behavior regresses.
5. Keep the change minimal: no new config, no schema changes, no unrelated sorting refactors.

Code guidance:
- Change only the comparator used by `sortByPriorityAndDate` in `Index.NextTodo()`.
- Preserve the existing priority and creation-time ordering semantics; the new rule is only a final deterministic tie-breaker when both are equal.
- Use exact equal timestamps in the regression test to avoid flakiness.
- Follow existing test style in `internal/storage/storage_test.go`.

Verification command:
- `go test ./internal/storage ./internal/task`

Commit message:
- `fix: break next-task ties by lower task id`