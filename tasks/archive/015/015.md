---
id: 15
title: Fix README.md documentation bugs
status: done
priority: high
type: bug
created_at: "2026-01-02T21:25:08Z"
updated_at: "2026-01-02T21:38:29Z"
---

Fix multiple issues in the README.md:

1. **Binary download section**: Add instruction to rename downloaded binary to `mcp-task-manager` to match usage examples elsewhere in the README.

2. **Add go install option**: Add `go install github.com/gpayer/mcp-task-manager/cmd/mcp-task-manager@latest` as an installation method.

3. **CLI path references**: Change all relative path references (`./mcp-task-manager`) to just `mcp-task-manager` (assuming it's in PATH).

4. **Claude Code integration - skill installation**: Remove the incorrect skill copying instructions. Replace with proper plugin installation:
   - Add the plugin marketplace: `claude plugin marketplace add github.com/gpayer/mcp-task-manager`
   - Install the plugin: either from inside Claude Code or via `claude plugin install <plugin-name>`
   - Read official Claude Code docs if needed for correct syntax.

5. **Claude Code integration - MCP server**: Fix the MCP server installation command. Correct syntax is:
   ```
   claude mcp add --transport stdio mcp-task-manager
   ```
   (assuming mcp-task-manager is in PATH)