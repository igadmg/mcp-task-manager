---
id: 50
parent_id: 49
title: 'Task 1: Add regression tests for empty-project startup'
status: done
priority: high
type: bug
created_at: "2026-03-26T21:21:31Z"
updated_at: "2026-03-26T21:41:42Z"
---

Files:
- Modify: `internal/storage/storage_test.go`
- Modify: `internal/task/service_test.go`

Steps:
1. Write failing tests for missing-index startup with no active tasks.
2. Run test to verify failure: `go test ./internal/storage ./internal/task -run 'TestIndex_Load|TestService_Initialize' -v`
3. Implement only the minimum test code needed to express the bug.
4. Re-run the targeted tests and confirm the new regression currently fails for the expected reason.
5. Commit with message: `test: add lazy index startup regression coverage`

Code:
- Add a storage-level test that constructs `MarkdownStorage` and `Index` against a temp tasks path with no `.md` files and no `.index.json`, calls `idx.Load()`, and asserts no error, no `.index.json`, and an empty `idx.All()` result.
- Add a storage-level test for an existing but empty tasks directory and assert `idx.Load()` still leaves `.index.json` absent.
- Add a service initialization regression test using real `MarkdownStorage` and `Index` with `cfg.ProjectFound = true`, call `svc.Initialize()`, and assert initialization does not create the tasks directory or `.index.json`.

Verification:
`go test ./internal/storage ./internal/task -run 'TestIndex_Load|TestService_Initialize' -v`