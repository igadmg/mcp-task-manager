---
id: 76
parent_id: 75
title: 'Task 1: Add Codex marketplace catalog and plugin manifest'
status: done
priority: high
type: feature
created_at: "2026-04-23T19:50:09Z"
updated_at: "2026-04-23T20:02:33Z"
---

Files:
- Create: `.agents/plugins/marketplace.json`
- Create: `plugins/mcp-task-manager/.codex-plugin/plugin.json`
- Reference: `.claude-plugin/marketplace.json`
- Reference: `.claude-plugin/plugin.json`

Steps:
1. Inspect the existing Claude marketplace/plugin metadata in `.claude-plugin/marketplace.json` and `.claude-plugin/plugin.json` so publisher name, repository URL, license, keywords, and version stay aligned where that still makes sense.
2. Create `.agents/plugins/marketplace.json` so this repository becomes a Codex marketplace root. Add top-level marketplace metadata (`name`, `interface.displayName`) and a single `plugins[]` entry for `mcp-task-manager`.
3. Point the marketplace entry at `./plugins/mcp-task-manager` using the local-source shape expected by Codex marketplace files, and include explicit `policy.installation`, `policy.authentication`, and `category` fields.
4. Create `plugins/mcp-task-manager/.codex-plugin/plugin.json` with complete plugin metadata for Codex discovery. Include the plugin name `mcp-task-manager`, version, description, author/publisher, repository, license, keywords, and an `interface` block sized for marketplace presentation.
5. Add relative component paths in `plugin.json` only for components that will actually exist after Task 2. Do not leave broken paths or placeholder references that point outside the plugin package.
6. Re-read both new JSON files to confirm the marketplace entry name matches the plugin folder and the plugin manifest name exactly.

Code guidance:
- Treat the repo root as the marketplace and `plugins/mcp-task-manager/` as the installable Codex plugin package.
- Keep the existing `.claude-plugin/` files intact unless a very small metadata sync is required later; this task is about adding Codex marketplace support, not removing Claude compatibility.
- Use the same normalized plugin identifier everywhere: folder name, marketplace entry name, and `plugin.json` `name` must all be `mcp-task-manager`.
- Prefer metadata values already used by this repository over inventing new branding.

Verification command:
- `sed -n '1,220p' .agents/plugins/marketplace.json && sed -n '1,260p' plugins/mcp-task-manager/.codex-plugin/plugin.json`

Commit message:
- `feat: add codex marketplace metadata for mcp-task-manager`