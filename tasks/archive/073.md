---
id: 73
parent_id: 70
title: 'Task 1: Add in-progress prioritization regression tests'
status: done
priority: high
type: bug
created_at: "2026-04-13T19:43:16Z"
updated_at: "2026-04-13T19:50:56Z"
---

Files:
- Modify: `internal/storage/storage_test.go`
- Reference: `internal/storage/index.go`

Steps:
1. Add focused `NextTodo` regression tests that reproduce the resumability bug.
2. Cover both a plain task and a subtask case where actionable `in_progress` work must beat otherwise-eligible `todo` work.
3. Keep the assertions aligned with current parent-aware ordering rules from Task `#67`, so the tests verify state precedence first and existing priority/age rules second.
4. Run the targeted `NextTodo` tests and confirm the new cases fail before implementation.
5. Leave production code unchanged in this subtask.

Code guidance:
- Add table-free, explicit tests near the existing `TestIndex_NextTodo...` coverage.
- One test should show an `in_progress` standalone task winning over a `todo` task.
- One test should show an `in_progress` subtask of an `in_progress` parent winning over a `todo` sibling or competing `todo` task.
- Build fixtures with deterministic `CreatedAt` values so ordering is unambiguous.
- Assert against the exact task IDs returned by `idx.NextTodo()`.

Verification command:
- `env GOCACHE=/tmp/go-build go test ./internal/storage -run 'TestIndex_NextTodo'`

Commit message:
- `test: cover in-progress task prioritization in next-task selection`