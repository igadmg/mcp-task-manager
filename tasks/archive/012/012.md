---
id: 12
parent_id: 9
title: 'Task 2: Update README with subtasks feature documentation'
status: done
priority: medium
type: feature
created_at: "2026-01-02T17:54:27Z"
updated_at: "2026-01-02T17:59:06Z"
---

**Files:**
- Modify: `README.md`

**Steps:**
1. Read the current README.md
2. Add a new "### Subtasks" section after the "### Priority Levels" section (around line 178)
3. Add the following content:

```markdown
### Subtasks

Tasks support single-level nesting via the `parent_id` field.

**Creating subtasks:**
```bash
# CLI
./mcp-task-manager create "Implement login form" -p high --parent 1

# MCP tool
create_task with parent_id parameter
```

**Automatic behaviors:**
- Starting a subtask auto-starts its parent task
- Completing the last subtask auto-completes the parent
- Parent tasks cannot be completed while subtasks remain incomplete
- `get_next_task` returns subtasks instead of parents with incomplete subtasks
```

4. Update the MCP Tools tables to mention subtask support:
   - `create_task`: add "and optional `parent_id` for subtasks"
   - `list_tasks`: add "; use `parent_id` filter for subtasks"
   - `get_task`: add "(includes subtasks for parent tasks)"
   - `delete_task`: add "; use `delete_subtasks` to cascade"

5. Update CLI Commands table to add `--parent` flag to create command description

6. Commit with message: `docs: add subtasks feature documentation`

**Verification:**
- README.md contains "### Subtasks" section
- MCP Tools table mentions subtask parameters
- CLI create command shows --parent flag