---
id: 60
parent_id: 54
title: 'Task 3: Update install and usage docs for repo-local agents'
status: done
priority: medium
type: feature
created_at: "2026-04-08T08:11:32Z"
updated_at: "2026-04-08T08:49:25Z"
---

Files:
- Modify: `.codex/INSTALL.md`
- Modify: `commands/execute-all.md`
- Modify: `README.md`

Steps:
1. Read the current docs: `sed -n '1,260p' .codex/INSTALL.md && sed -n '1,220p' commands/execute-all.md && sed -n '132,180p' README.md`
2. Update `.codex/INSTALL.md` so installation links both top-level `skills/` and top-level `agents/` into Codex discovery paths, including verification and uninstall steps for both.
3. Update `commands/execute-all.md` so it describes the agent-aware workflow rather than only generic subagent-driven execution.
4. Update the Codex-facing workflow wording in `README.md` so it matches the role-agent behavior and fallback approval rule.
5. Re-read the updated docs: `sed -n '1,260p' .codex/INSTALL.md && sed -n '1,220p' commands/execute-all.md && sed -n '132,180p' README.md`
6. Commit with message: `docs: document repo-local workflow agents`

Code:
- The install instructions should remain clone-plus-symlink/junction based, but they must expose both repo `skills/` and repo `agents/`.
- The usage docs should state that the workflow uses repo-local `planner`, `coder`, and `reviewer` agents when available and asks the user before any fallback.
- Keep wording consistent across install instructions, command help, and README integration notes.

Verification:
`sed -n '1,260p' .codex/INSTALL.md && sed -n '1,220p' commands/execute-all.md && sed -n '132,180p' README.md`