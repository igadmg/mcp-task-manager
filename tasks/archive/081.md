---
id: 81
parent_id: 79
title: 'Task 1: Development Workflow Role Prompts'
status: done
priority: high
type: feature
created_at: "2026-07-10T20:21:18Z"
updated_at: "2026-07-10T20:26:52Z"
---

Implement the implicit role-prompt dispatch foundation in the development workflow skill.

Files:
- Modify: `skills/superpowers-workflow/SKILL.md`
- Read/source role rules from: `plugins/mcp-task-manager/agents/planner.toml`, `plugins/mcp-task-manager/agents/coder.toml`, `plugins/mcp-task-manager/agents/reviewer.toml`
- Reference: `docs/plans/2026-07-10-implicit-subagent-roles-migration.md`

Steps:
1. Replace the workflow overview so it describes ordinary spawned subagents with self-contained role prompts, not Codex custom role agents or globally installed TOML agents.
2. Remove the `Agent Resolution Policy` content that prefers `planner`, `coder`, and `reviewer` custom agents and asks for `$install-agents` fallback remediation.
3. Rewrite the planning dispatch section so `spawn_agent` uses a descriptive `task_name` such as `plan_task_{id}`, `fork_turns: "none"`, and a complete `message` containing the planner role contract.
4. The planner prompt text must include the exact identity `You are the planning-only agent for this assignment`, parent task ID/title/priority/type/description, use of `writing-plans`, permission to create only requested child tasks with `create_task`, required subtask fields, and prohibitions on production-file edits, implementation, review substitution, task completion, git operations, and subagent delegation.
5. Rewrite the coder dispatch section so `spawn_agent` uses a descriptive `task_name` such as `code_task_{id}`, `fork_turns: "none"`, and a complete `message` containing the coder role contract.
6. The coder prompt text must include the exact identity `You are the implementation-only agent for this assignment`, subtask and parent identifiers, full task specification, permission to edit only files needed by the assigned implementation, execution/verification skill guidance, status vocabulary `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, `BLOCKED`, and prohibitions on task-manager mutations, commits, unrelated cleanup, planning expansion, self-approval, and subagent delegation.
7. Rewrite the reviewer dispatch section so `spawn_agent` uses a descriptive `task_name` such as `review_task_{id}`, `fork_turns: "none"`, and a complete `message` containing the reviewer role contract.
8. The reviewer prompt text must include the exact identity `You are the review-only agent for this assignment`, original spec and diff/repository context expectations, spec-compliance-first review behavior, severity and file-reference requirements, explicit approval wording when no findings remain, and prohibitions on editing files, fixing findings, mutating tasks, committing, replanning without a genuine plan defect, and subagent delegation.
9. Preserve the workflow controller ownership language: the controller owns task selection, task state changes, phase transitions, commits, and user communication.
10. Preserve sequential behavior: after every `spawn_agent`, the controller must immediately `wait_agent` for that exact subagent before taking any other workflow step.

Code guidance:
- Treat the initial `message` as the full role definition for that invocation, not supplemental instructions.
- Carry forward every material rule from the TOML `developer_instructions`; do not rely on role names to imply behavior.
- Keep the skill instructions practical and procedural, since this is documentation that drives agent behavior rather than Go code.
- Do not edit packaged copies in this task; synchronization is handled by a later subtask.

Verification command:
```bash
rtk rg -n 'planning-only agent|implementation-only agent|review-only agent|fork_turns: "none"|install-agents|custom role agent|custom agent|\.codex/agents|~/.codex/agents' skills/superpowers-workflow/SKILL.md | head -c 6000
```
Expected: the three implicit role identities and `fork_turns: "none"` are present; obsolete custom-agent/install references are absent from the development workflow skill.

Commit message:
```text
feat(workflow): embed implicit subagent role prompts
```