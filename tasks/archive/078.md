---
id: 78
parent_id: 75
title: 'Task 3: Document marketplace installation and plugin usage'
status: done
priority: high
type: feature
created_at: "2026-04-23T19:50:49Z"
updated_at: "2026-04-23T20:20:57Z"
---

Files:
- Modify: `README.md`
- Modify: `.codex/INSTALL.md`
- Modify: `commands/execute-all.md`
- Reference: `.agents/plugins/marketplace.json`
- Reference: `plugins/mcp-task-manager/.codex-plugin/plugin.json`
- Reference: `plugins/mcp-task-manager/.mcp.json`
- Reference: `plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md`

Steps:
1. Read the current Codex and Claude integration sections in `README.md` plus the current install flow in `.codex/INSTALL.md` so the new marketplace instructions replace or refine the existing manual clone/symlink guidance without creating contradictory paths.
2. Update `README.md` to explain that this repository now acts as a Codex marketplace, how to add the marketplace, how to install `mcp-task-manager`, and any separate MCP server prerequisite that still applies.
3. Update `.codex/INSTALL.md` to document the marketplace-based installation flow in a way that is reproducible. If any manual steps remain necessary after plugin installation, keep them explicit and in the correct order.
4. Update `commands/execute-all.md` if its usage text still assumes the old install surface, so the command/help wording matches the packaged plugin and current workflow behavior.
5. Make sure the Codex instructions are internally consistent about plugin name, marketplace source, installed asset location, and whether role agents are provided by the plugin package, existing `.codex/agents/*.toml`, or a separate setup step.
6. Re-read the touched documentation and command text end-to-end to remove stale references to the pre-marketplace setup where they would confuse installation.

Code guidance:
- Prefer one primary Codex installation path in the docs: marketplace add/install first, then any required MCP or agent configuration.
- Preserve useful Claude Code documentation, but clearly separate it from the new Codex marketplace flow.
- Do not claim the plugin auto-configures behavior that is still manual; if MCP server registration or custom-agent setup remains manual, say so explicitly.
- Keep command examples concrete and aligned with the actual plugin/package names introduced in Tasks 1 and 2.

Verification command:
- `rg -n "marketplace|plugin install|codex mcp add|mcp-task-manager@mcp-task-manager|plugins/mcp-task-manager|\.mcp.json" README.md .codex/INSTALL.md commands/execute-all.md`

Commit message:
- `docs: add codex marketplace install instructions`