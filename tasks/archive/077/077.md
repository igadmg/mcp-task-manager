---
id: 77
parent_id: 75
title: 'Task 2: Package Codex plugin content for discovery and use'
status: done
priority: high
type: feature
created_at: "2026-04-23T19:50:33Z"
updated_at: "2026-04-23T20:10:21Z"
---

Files:
- Modify: `plugins/mcp-task-manager/.codex-plugin/plugin.json`
- Create: `plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md`
- Create: `plugins/mcp-task-manager/commands/execute-all.md`
- Create: `plugins/mcp-task-manager/.mcp.json`
- Reference: `skills/superpowers-workflow/SKILL.md`
- Reference: `commands/execute-all.md`
- Reference: `.codex/agents/planner.toml`
- Reference: `.codex/agents/coder.toml`
- Reference: `.codex/agents/reviewer.toml`

Steps:
1. Read the existing Codex-facing workflow assets in `skills/superpowers-workflow/SKILL.md`, `commands/execute-all.md`, and `.codex/agents/*.toml` to identify what the plugin package must expose to stay usable after marketplace installation.
2. Create `plugins/mcp-task-manager/skills/superpowers-workflow/SKILL.md` as the plugin-local skill entrypoint. Base it on the current workflow skill, but make the packaged version self-contained and consistent with the plugin install surface.
3. Create `plugins/mcp-task-manager/commands/execute-all.md` so the packaged plugin exposes the workflow command/help text inside the plugin directory rather than only at repo root.
4. Create `plugins/mcp-task-manager/.mcp.json` so the plugin can declare the `task-manager` MCP server integration surface. Keep the command conservative: rely on the existing `mcp-task-manager` executable or documented binary path rather than inventing a new bootstrap script.
5. Update `plugins/mcp-task-manager/.codex-plugin/plugin.json` to reference the packaged `skills` path and the packaged `mcpServers` path. If command metadata belongs in the plugin package for Codex discovery, wire it up there as well.
6. Confirm the plugin package contains all files referenced by the manifest and that no manifest path points back to repo-root-only assets.

Code guidance:
- Make the plugin package self-contained enough that Codex can discover the plugin from `plugins/mcp-task-manager/` without depending on hidden repo conventions.
- Reuse the current workflow wording and command semantics where possible; only change text that must change because the assets now live inside the packaged plugin.
- Do not add new production dependencies or invent automation outside the repository’s existing binary/runtime assumptions.
- Keep agent behavior references aligned with the current `.codex/agents/*.toml` role definitions, even if those agent files themselves remain outside the plugin manifest.

Verification command:
- `find plugins/mcp-task-manager -maxdepth 4 -type f | sort && sed -n '1,260p' plugins/mcp-task-manager/.codex-plugin/plugin.json && sed -n '1,220p' plugins/mcp-task-manager/.mcp.json`

Commit message:
- `feat: package mcp-task-manager as a codex plugin`