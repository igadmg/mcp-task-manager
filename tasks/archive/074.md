---
id: 74
parent_id: 70
title: 'Task 2: Make get_next_task prefer actionable in-progress work'
status: done
priority: high
type: bug
created_at: "2026-04-13T19:43:22Z"
updated_at: "2026-04-13T19:49:04Z"
---

Files:
- Modify: `internal/storage/index.go`
- Verify against: `internal/storage/storage_test.go`

Steps:
1. Review the current `NextTodo` selection logic and identify where task status filtering happens before ordering.
2. Change the algorithm so actionable `in_progress` tasks are considered before `todo` tasks.
3. Preserve the ordering semantics within each state bucket:
   - `in_progress` groups should still respect parent-aware grouping for subtasks.
   - `todo` groups should continue to use the parent-inherited ordering introduced for Task `#67`.
4. Ensure blocked tasks are still excluded and parents with incomplete subtasks still resolve to actionable child work where applicable.
5. Run the focused `NextTodo` tests, then the full storage test package.

Code guidance:
- Keep the change local to `NextTodo` and any small helper functions it needs.
- Treat task state as a primary ordering dimension: actionable `in_progress` first, actionable `todo` second.
- For subtasks, apply the same parent-group ranking model in both state buckets so resumed work on an active parent behaves consistently.
- Do not broaden semantics beyond `NextTodo`; this task is about selection order, not changing task transitions.

Verification command:
- `env GOCACHE=/tmp/go-build go test ./internal/storage -run 'TestIndex_NextTodo'`
- `env GOCACHE=/tmp/go-build go test ./internal/storage`

Commit message:
- `fix: prioritize in-progress work in next-task selection`